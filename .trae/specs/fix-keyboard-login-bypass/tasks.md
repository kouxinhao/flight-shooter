# Tasks

- [x] Task 1: 在 keydown 处理器中添加未登录守卫 — 在 `gameState === 'menu'` 分支中，`loginTransitionGuard` 检查之前，添加 `if (!loggedIn) return;`
  - [x] 在 `script.js` 第 457 行前插入 `if (!loggedIn) return;`
  - [x] 确认已登录玩家仍可通过键盘正常开始游戏

# Task Dependencies
- 无依赖