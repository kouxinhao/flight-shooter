# Tasks

- [x] Task 1: 修改 `onLogout()` 函数 — 退出登录后重置游戏状态并返回菜单界面
  - [x] 在 `onLogout()` 中清除游戏实体（敌人、子弹、道具、粒子、Boss等）
  - [x] 重置游戏相关变量（分数、连击、武器等级、阶段等）
  - [x] 设置 `gameState = 'menu'`
  - [x] 确保 BGM 正确切换回菜单 BGM

# Task Dependencies

无依赖项，单文件修改